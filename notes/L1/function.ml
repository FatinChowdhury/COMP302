let greet name = "hello " ^ name ;;
val greet : string -> string = <fun>
            (input)   (output)

// to run the function
greet "fatin" ;;
- : string = "hello fatin"

greet name ;;
- : string = "hello nitaf"
