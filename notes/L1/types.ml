"hello " ^ "world" ;; 
(would return)
- : string = "hello world"
let name = "fatin" ;;
val name : string = "fatin"    (the : means "has type")


let name = "nitaf" ;;
val name : string = "nitaf"


name ;; 
- : string = "fatin"
(it uses most recent value of the "variable")
